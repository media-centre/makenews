/* eslint max-nested-callbacks: [2, 5] */
"use strict";

//import { displayParkedFeedsAsync } from "../../../src/js/park/actions/ParkActions";
//import mockStore from "../../helper/ActionHelper.js";


xdescribe("displayParkedFeedsAsync", () => {
    //it("dispatch displayParkedFeedsAsync", (done) => {
    //    let feeds = [];
    //    let store = mockStore({ "feeds": [] }, [{ "type": DISPLAY_ALL_FEEDS, "feeds": feeds }], done);
    //    return Promise.resolve(store.dispatch(displayParkedFeedsAsync())).then(() => {
    //
    //    });
    //});
});
